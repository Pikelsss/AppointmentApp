<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sign Up</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    .bg-gradient {
      background: linear-gradient(135deg, #667eea, #764ba2);
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(-20px);
      }

      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .fade-in {
      animation: fadeIn 1s ease-out;
    }

    @keyframes pulse {

      0%,
      100% {
        transform: scale(1);
      }

      50% {
        transform: scale(1.05);
      }
    }

    .pulse:hover {
      animation: pulse 0.5s ease-in-out;
    }
  </style>
</head>

<body class="flex items-center justify-center h-screen bg-gradient">
  <div class="bg-white shadow-xl border border-gray-300 w-[600px] p-8 relative slide-in rounded-lg">
    <h1 class="text-3xl font-bold text-gray-800 mb-8">Create Account</h1>
    <%= form_with(model: @user, url: new_user_registration_path, local: true) do |form| %>
    <div class="mb-4">
      <%= form.label :username, class: "block text-sm font-medium text-gray-600 mb-1" %>
      <%= form.text_field :username, class: "w-full border border-gray-300 rounded-md p-3 focus:ring-2 focus:ring-blue-400 focus:outline-none", required: true %>
    </div>
    <div class="mb-4">
      <%= form.label :email, class: "block text-sm font-medium text-gray-600 mb-1" %>
      <%= form.email_field :email, class: "w-full border border-gray-300 rounded-md p-3 focus:ring-2 focus:ring-blue-400 focus:outline-none", required: true %>
    </div>
    <div class="mb-4">
      <%= form.label :password, class: "block text-sm font-medium text-gray-600 mb-1" %>
      <%= form.password_field :password, class: "w-full border border-gray-300 rounded-md p-3 focus:ring-2 focus:ring-blue-400 focus:outline-none", required: true %>
    </div>
    <div class="mb-4">
      <%= form.label :password_confirmation, class: "block text-sm font-medium text-gray-600 mb-1" %>
      <%= form.password_field :password_confirmation, class: "w-full border border-gray-300 rounded-md p-3 focus:ring-2 focus:ring-blue-400 focus:outline-none", required: true %>
    </div>
    <div>
      <%= form.submit "Sign Up", class: "w-full bg-blue-500 text-white py-3 rounded-md hover:bg-blue-600 transition font-semibold pulse" %>
    </div>
    <% end %>
  </div>

</html>